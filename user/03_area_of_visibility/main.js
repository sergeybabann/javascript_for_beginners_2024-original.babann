// Переменные типа let и const имеют область видимости внутри блока кода.

{
    let userName = "Bob"
    console.log(userName)
}

console.log(userName) // за пределами блока кода. за пределами фигурных скобок